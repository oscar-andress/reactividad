[project]
name = "reactividad-mcp-server"
version = "0.1.0"
description = "MCP server exposing the reactividad menu API as tools"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.8.0,<2",
    "httpx>=0.27.0",
]
